# GitHub profile README

This repo holds only `README.md`, rendered on github.com/oleksiisedun.

- The "How I work with AI" section summarizes `~/.claude/CLAUDE.md`. When editing it, check each bullet against that file: every claim must be accurate to it, but bullets stay short summaries, not a copy.
- Links are checked in CI (`.github/workflows/links.yml`, weekly and on push). Run locally with `lychee README.md` if lychee is installed.
